language: node_js
node_js:
  - "stable"
env:
  - NODE_ENV=DBTRAVIS_URL
cache:
  directories:
    - node_modules
    - server/node_modules
    - client/node_modules
script:
  - cd client && npm i && npm run travis:test && cd ../server && npm i && npm run travis:test
